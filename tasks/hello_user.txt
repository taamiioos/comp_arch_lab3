.data:
    str STRING Hello,
    buf BUFFER 64

.code:
LOAD r1, user
read:
    INPUT [r1]
    INC r1
    CMP [r1], 0
    JZ end_read
    JMP read
end_read:
    DEC r1

LOAD r1, str
print:
    OUTPUT [r1]
    INC r1
    CMP [r1], 0
    JZ end_print
    JMP print
end_print:

LOAD r1, user
print_user:
    OUTPUT [r1]
    INC r1
    CMP [r1], 0
    JZ end_print_user
    JMP print_user

end_print_user:
LOAD r1, 0
OUTPUT [r1]

HLT

.data
    str STRING hello world

.code
    ; загрузить адрес строки str в регистр r1
    LOAD r1, str

print:
    ; загрузить символ из текущей ячейки памяти в регистр r2
    LOAD r2, [r1]

    ; вывести символ на экран
    OUTPUT r2

    ; проверить, достигнут ли конец строки
    CMP r2, 0
    JZ print_end

    ; увеличить указатель на следующую ячейку памяти
    INC r1
    JMP print_string

end:
    ; остановить выполнение программы
    HLT
